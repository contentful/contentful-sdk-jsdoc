version: 2.1
jobs:
  release:
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - run: npm install
      - run: npm run semantic-release
workflows:
  version: 2
  build_and_test:
    jobs:
      - release:
          context: dev-workflows-release
